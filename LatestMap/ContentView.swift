//
//  ContentView.swift
//  LatestMap
//
//  Created by Jason Marsellino on 30/05/26.
//

import SwiftUI

// REVIEW: Xcode template leftover — unused; the app's real root is `RootTabView` (see LatestMapApp).
/// Placeholder "Hello, world!" view; safe to delete.
struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
